1.default와 public만 사용가능
예) class 클래스{..}==>default 접근제한
	 public class 클리스 {..}==>public 접근제한
2. default 접근 제한
1)정의:클래스를 선언할 때 public 을 생략했다면 클래스는 default접근제한을 가짐
2)이러한 경우 같은 패키지에서는 아무 제한 없이 사용할 수 있지만 
	다른 패키지에서는 사용할수 없도록 제한된다.
3.public 접근제한
1)정의: 클래스를 선언할때 public접근 제한자를 붙였다면 클래스를 이러한 	
		시야를 가지게 된다.
2) public의 경우 같은 패키지 뿐 아니라 다른 패키지에서도 아무런 제한 없이 
사용 가능하다.
3) 라이브러리 클래스를 개발한다면 반드시 public접근 제한을 설정해준다.
예)인터넷으로 배포되는 라이브러리 클래스들도 모두 public 접근제한을 가짐