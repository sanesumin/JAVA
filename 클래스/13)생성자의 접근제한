1.생성자가 어떤 접근 제한을 갖느냐에 따라 호출 가능 여부가 결정됨.
	(객체 생성 여부가 결정되는데 중요한 역할)

2. public, protected, default, private 접근 제한을 모두 가질 수 있음.
3. 자동생성 디폴트 생성자는 클래스의 접근제한과 동일한 접근성을 가진다.
	예) 클래스가 public이면 디폴트 생성자도 public이다.
	클래스가 default이면 디폴트 생성자도 default이다.
	혹시 public 디폴트 생성자라고 명시되었어도 클래스와
	동등하게 default로 취급된다.
4.protected 생성자의 경우
	시야가 default생성자와 같은데, 다른점은 다른 패키지에 있는 클래스가
	본인의 클래스에서 상속된 자식 클래스인 경우 이 생성자를 사용 가능.
5.default 생성자의 경우
	public,private등을 생략한 경우 이 경우의 생성자라 명명한다.
	같은 패키지에서는 자유롭게 사용가능한 생성자이지만, 다른 패키지에서는
	호출하여 사용할수가 없다.
6.private 생성자의 경우
	동일 패키지이던지 다른 패키지이던지 내 클래스가 아니면 무조건
	본 생성자를 호출하지 못하도록 제한한다. 오로지 본 클래스 내부에서만
	생성자를 호출 가능하고 객체를 만들수 있다.